export declare const svgNS = "http://www.w3.org/2000/svg";
/** Left padding of SVG. */
export declare const leftPadding = 20;
/** x coordinate for first operation on each register. */
export declare const startX = 80;
/** y coordinate of first register. */
export declare const startY = 40;
/** Minimum width of each gate. */
export declare const minGateWidth = 40;
/** Height of each gate. */
export declare const gateHeight = 40;
/** Padding on each side of gate. */
export declare const gatePadding = 10;
/** Padding on each side of gate label. */
export declare const labelPadding = 10;
/** Height between each qubit register. */
export declare const registerHeight: number;
/** Height between classical registers. */
export declare const classicalRegHeight: number;
/** Group box inner padding. */
export declare const groupBoxPadding = 10;
/** Padding between nested groups. */
export declare const nestedGroupPadding = 2;
/** Additional offset for control button. */
export declare const controlBtnOffset = 40;
/** Control button radius. */
export declare const controlBtnRadius = 15;
/** Default font size for gate labels. */
export declare const labelFontSize = 14;
/** Default font size for gate arguments. */
export declare const argsFontSize = 12;
/** Starting x coord for each register wire. */
export declare const regLineStart = 40;
